## No Requirements Changes

This change is a performance optimization with no functional impact:

- No new requirements added
- No existing requirements modified
- No requirements removed

The behavior of move list handling remains identical; only the internal storage mechanism changes to use stack-allocated small vectors.